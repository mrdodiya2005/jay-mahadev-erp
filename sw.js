self.addEventListener('install', (e) => {
  console.log('[Service Worker] Install');
});

self.addEventListener('fetch', (e) => {
  // Aa ekdam basic service worker che. 
  // PWA na rules pass karva mate aatlu purtu che.
});
